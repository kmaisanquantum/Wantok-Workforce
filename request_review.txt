I have completely removed the category restriction logic from the Customer Module search.

Changes:
1. WantokWorkforce.js: Removed CATEGORY_SYNONYMS and updated fetchNearbyProviders to use the generic 'search' parameter.
2. match_controller.js: Removed categorySynonyms and normalizeSearchTrade. Updated getNearbyWorkers to handle the 'search' parameter and pass it to the service.
3. match_service.js: Expanded textSearchWorkers and findNearbyWorkers to include 'role' and 'bio' in the search filters, allowing for a broad text match across all worker fields.

Verified the build remains clean and the code is syntactically correct.
