I have moved the broad search logic to the frontend to fix the internal server error and maintain compatibility with the backend's expected parameter schema.

Changes:
1. Reverted backend MatchController and MatchService to their original state, but ensured the SELECT query returns 'role' and 'bio' fields.
2. Updated fetchNearbyProviders in WantokWorkforce.js:
   - It now fetches all workers from the base endpoint (no search/category params).
   - Applies a broad text filter on the results (name, role, bio, category, skills).
   - Handles errors gracefully by setting an empty array and removing alerts.
3. Verified the build remains clean.
