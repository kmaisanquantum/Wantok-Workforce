I have updated the search functionality to be fully open across all text fields.

Changes:
1. WantokWorkforce.js: fetchNearbyProviders now uses the 'keyword' parameter. It implements a broad frontend filter matching against title, name, bio, category, and skills.
2. match_controller.js: Added support for the 'keyword' query parameter to the backend.
3. Verified the build is clean and logic is sound.
